import { config as button } from './button';
import { config as container } from './container';
import { config as img } from './img';
import { config as news } from './news';
import { config as overlay } from './overlay';
import { config as page } from './page';
import { config as qrcode } from './qrcode';
import { config as text } from './text';

export default {
  page,
  container,
  button,
  text,
  img,
  qrcode,
  overlay,
  news,
};

